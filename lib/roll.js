export default function roll(num_sides, num_dice, num_rolls) {
    
    let returnObj = {
        sides : num_sides,
        dice : num_dice,
        rolls : num_rolls
    }

    return returnObj;
}